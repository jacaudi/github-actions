# Dev / feature branch pipeline
# Runs on every push to non-main branches.
name: CI (Dev)

on:
  push:
    branches-ignore: [main]

jobs:
  pipeline-summary:
    if: always()
    uses: jacaudi/github-actions/.github/workflows/pipeline-summary.yml@main
